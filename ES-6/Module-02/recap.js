//spread

const a=110;
const arr=[12,23,56];
const obj ={
    name:'adnan rony',
};

const final =`mu name is ${obj.name} and i have some extra ${a} money and my pen price is ${arr[1]}`

console.log(final);